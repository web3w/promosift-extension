{
  "name": "promosift-extension",
  "version": "1.0.3",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check background.js && node --check popup.js && node --check content.js",
    "test": "node --test tests/*.test.cjs",
    "configure": "node scripts/configure-extension.mjs",
    "package": "node scripts/package-extension.mjs"
  }
}
